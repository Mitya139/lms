 # apps/tasks — task helpers and abstractions

 Purpose
 - Common contracts/base classes for domain background jobs.

 Layout
 - __init__.py, apps.py (when present)
 - tasks/*.py — task sets per domain or shared base classes.
 - tests/ — when present.
